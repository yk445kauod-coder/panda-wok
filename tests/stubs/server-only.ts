// Stub for the `server-only` package, which Next.js aliases internally and is
// not installed as a real dependency. Vitest needs a resolvable module so the
// modules under test can import it unchanged.
export {};
